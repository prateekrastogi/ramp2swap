{
	"name": "partner-web",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"check:design-system": "node ./scripts/check-design-system.mjs",
		"build": "npm run check:design-system && astro build",
		"preview": "npm run check:design-system && astro build && wrangler dev --config ./.wrangler/deploy/config.json",
		"astro": "astro",
		"deploy": "npm run check:design-system && astro build && wrangler deploy --config ./.wrangler/deploy/config.json",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^13.1.1",
		"astro": "^6.0.4"
	},
	"devDependencies": {
		"@types/node": "^25.5.0",
		"wrangler": "^4.73.0"
	}
}

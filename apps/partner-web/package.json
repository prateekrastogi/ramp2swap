{
	"name": "partner-web",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"check:design-system": "node ./scripts/check-design-system.mjs",
		"build": "npm run check:design-system && astro build",
		"preview": "astro build && wrangler dev",
		"astro": "astro",
		"deploy": "npm run check:design-system && astro build && wrangler deploy",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^12.6.12",
		"astro": "^5.17.1"
	},
	"devDependencies": {
		"@types/node": "^25.3.5",
		"wrangler": "^4.71.0"
	}
}

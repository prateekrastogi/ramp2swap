{
	"name": "partner-web",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"setup:auth-local": "node ../../scripts/setup-partner-auth-local.mjs",
		"dev": "npm run setup:auth-local && ASTRO_DISABLE_CLOUDFLARE_ADAPTER=true astro dev",
		"check:design-system": "node ./scripts/check-design-system.mjs",
		"build": "npm run check:design-system && astro build",
		"preview": "npm run check:design-system && astro build && wrangler dev --config ./wrangler.deploy.jsonc",
		"astro": "astro",
		"deploy": "npm run check:design-system && astro build && wrangler deploy --config ./wrangler.deploy.jsonc",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@astrojs/cloudflare": "^13.1.1",
		"astro": "^6.0.4"
	},
	"devDependencies": {
		"@types/node": "^25.5.0",
		"wrangler": "^4.73.0"
	}
}

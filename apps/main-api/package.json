{
  "name": "main-api",
  "type": "module",
  "scripts": {
    "check:design-system": "node ./scripts/check-design-system.mjs",
    "sync-system-prompt": "node ./scripts/sync-system-prompt.mjs",
    "predev": "npm run sync-system-prompt",
    "dev": "wrangler dev",
    "predeploy": "npm run check:design-system && npm run sync-system-prompt",
    "deploy": "wrangler deploy --minify",
    "precf-typegen": "npm run check:design-system && npm run sync-system-prompt",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.12.7"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "wrangler": "^4.73.0"
  }
}

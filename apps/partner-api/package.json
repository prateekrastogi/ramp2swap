{
	"name": "partner-api",
	"type": "module",
	"scripts": {
		"dev": "node ../../scripts/setup-partner-auth-local.mjs && wrangler d1 migrations apply AUTH_DB --local && node ./test/ensure-test-bench.mjs --env local && wrangler dev --local",
		"deploy:staging": "node ./scripts/check-design-system.mjs && node ./scripts/deploy-with-migrations.mjs --env staging --minify",
		"deploy": "node ./scripts/check-design-system.mjs && node ./scripts/deploy-with-migrations.mjs --minify"
	},
	"dependencies": {
		"hono": "^4.12.7",
		"unique-names-generator": "^4.7.1"
	},
	"devDependencies": {
		"@types/node": "^25.5.0",
		"wrangler": "^4.73.0"
	}
}
